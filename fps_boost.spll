PROCESSOR(GET)

IF VRAM+140MB = TRUE {FPS GAIN = ENABLED}

ELSE {FPS GAIN = DISABLED}

VR FPS GAIN = VIDEO_COFIG~ON.+140MB VERIFY<IF AND ELSE> SET FPS 'BOOST'

VR BOOST = VIDEO_PARAM SETMAX; LOAD FPS GAIN ON START

@export mythcoder.epizy.com/fps_boost.spllfyt